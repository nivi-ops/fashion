import 'package:flutter/material.dart';

/// ---------------------------------------------------------------------
/// COLORS — matches website.html :root CSS variables
/// ---------------------------------------------------------------------
class AppColors {
  static const primary = Color(0xFF008080);
  static const primaryLight = Color(0xFF20B2AA);
  static const primaryDark = Color(0xFF006666);
  static const secondary = Color(0xFFB87333);
  static const secondaryLight = Color(0xFFD4A574);
  static const gold = Color(0xFFD4AF37); // Shop Now / CTA buttons
  static const goldDark = Color(0xFF9C7A1E); // text on gold
  static const dark = Color(0xFF1A2A3A);
  static const light = Color(0xFFF0FFF4);
  static const text = Color(0xFF2D3748);
  static const textLight = Color(0xFF718096);
  static const gray = Color(0xFFF0F4F0);
  static const success = Color(0xFF28A745);
  static const danger = Color(0xFFDC3545);
}